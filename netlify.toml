# Netlify configuration for INDEX Systems
# Redirects /api/contact to the Netlify Function

[[redirects]]
  from   = "/api/contact"
  to     = "/.netlify/functions/contact"
  status = 200

[build]
  functions = "netlify/functions"
